import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Register - Chatty",
  description: "Create a new account to start chatting",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
